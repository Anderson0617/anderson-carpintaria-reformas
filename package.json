{
  "name": "anderson-carpintaria-reformas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "yarn run build && wrangler dev",
    "deploy": "yarn run build && wrangler deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.33.1",
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0",
    "wrangler": "^4.84.1"
  }
}
